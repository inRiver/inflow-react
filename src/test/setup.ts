import '@testing-library/jest-dom/vitest';

// Node's experimental built-in `localStorage` (enabled by default in some
// Node versions/flags) can shadow jsdom's own `window.localStorage`
// implementation, breaking `.getItem`/`.setItem` calls in components that
// rely on it. Force a deterministic in-memory polyfill for tests.
class MemoryStorage implements Storage {
  private store = new Map<string, string>();

  get length(): number {
    return this.store.size;
  }

  clear(): void {
    this.store.clear();
  }

  getItem(key: string): string | null {
    return this.store.has(key) ? this.store.get(key)! : null;
  }

  key(index: number): string | null {
    return Array.from(this.store.keys())[index] ?? null;
  }

  removeItem(key: string): void {
    this.store.delete(key);
  }

  setItem(key: string, value: string): void {
    this.store.set(key, String(value));
  }
}

Object.defineProperty(globalThis, 'localStorage', {
  value: new MemoryStorage(),
  writable: true,
  configurable: true,
});
